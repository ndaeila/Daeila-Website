# OS
.DS_Store
Thumbs.db
**/.DS_Store

# Hugo build output & cache
/public/
/resources/
.hugo_build.lock

# Hugo generated metadata
jsconfig.json
hugo_stats.json

# Editor / tooling cache
.dist
.tmp
.sass-cache
node_modules/
npm-debug.log
builds/

# NOTE: go.sum IS tracked (needed for reproducible Hugo module builds).
# NOTE: package-lock.json IS tracked when present.

# CMS OAuth secrets (live only on the server, NEVER commit)
cms/secrets.env
